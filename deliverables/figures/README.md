# Figures

Chart images exported by `save_chart()` in `notebooks/northstar_build.ipynb`.

- `<name>.png` — 2x PNG for the slide deck and written deliverables
- `<name>.txt` — the one-line message that figure is meant to prove

Tracked in git because presentation assets must survive a clean checkout. The
regenerable Vega-Lite specs live in `data/generated/charts/`, which is ignored.
