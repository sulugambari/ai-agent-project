# Final Presentation — Slide Deck Ledger

Running capture of everything that must appear in the final deck. **Appended to as
each step completes**, so the deck is assembled from evidence rather than
reconstructed from memory on the last day.

- **Figures:** `deliverables/figures/*.png` (2x PNG, tracked in git). Each figure
  has a sibling `.txt` holding the one-line message it proves.
- **Chart specs:** `data/generated/charts/*.json` (git-ignored, regenerable) —
  used by the Phase 8 Streamlit dashboard.
- **Companion:** `SHOWCASE.md` holds the *live demonstration* script; this file
  holds the *slides*. They overlap deliberately at the demo section.

## Deck Structure

| # | Slide | Content source | Status |
| --- | --- | --- | --- |
| 1 | Title — product name, team, date | — | Pending |
| 2 | The problem: one question, four sources | Acme Freight scenario, `PROJECT_PLAN.md` | Pending |
| 3 | Northstar Labs and its data landscape | Step 1.1 corpus + access figures | Figures ready |
| 4 | Who we built for, and what we refuse | `PRODUCT_BRIEF.md`, Step 1.2-1.3 | Figure ready |
| 5 | The information boundary | `ACCESS_MATRIX.md`, Steps 2.1-2.2, **3.2 counterfactual** | **Ready** |
| 6 | Architecture, and the alternatives we rejected | Step 2.3, `D-001`, **`D-002` + threat-model figure** | Figure ready |
| 7 | Baseline: what transparent lexical search cannot do | Step 3.3 figures | **Ready** |
| 8 | Connecting a live source safely | Step 4.3 figures | **Ready** |
| 9 | Retrieval: lexical vs semantic vs hybrid | Step 5.5 figures | Pending |
| 10 | Index lifecycle: change and deletion | Step 5.4, EVAL-011 · **design + figure ready from 2.2** | Figure ready |
| 11 | Five narrow tools, one bounded agent | Step 6.1-6.2 figures | Pending |
| 12 | Trust boundary: refusal, conflict, injection | Step 6.5, EVAL-005/006 · **threat model 2.3** | Partly sourced |
| 13 | Human approval before any action | Step 6.4 state diagram | Pending |
| 14 | Evaluation results by layer | Step 8.4 figures | Pending |
| 15 | What failed, and what we learned | Step 8.5 failure analysis | Pending |
| 16 | Release recommendation | Step 10.2, `DECISIONS.md` | Pending |
| 17 | What real deployment would still require | `SHOWCASE.md` launch boundary | Pending |

## Step Ledger

Appended as each step completes. `Figure` names map to
`deliverables/figures/<name>.png`.

| Step | Key finding worth presenting | Figure(s) | Slide |
| --- | --- | --- | --- |
| 0.3 | The lexical baseline is not a strawman — it already retrieves all three EVAL-002 expected sources. Phase 5 must beat something real. | *(none — table evidence)* | 7 |
| 0.3 | The teaching database is reproducible at row level but not byte level; SQLite page layout varies per run. | *(none)* | — *(technical note only)* |
| 0.3 | Streamlit binds all network interfaces by default and advertised a LAN URL — an unauthenticated assistant should not be LAN-reachable. | *(none)* | 17 |
| 1.1 | Permissions are **per-record metadata, not a role hierarchy**. Engineering sees the most records yet cannot see the refund policies or the customer-operations thread; Customer Success sees the policies but not the engineering blockers. No single role can answer a cross-domain question alone. | `1_1_access_heatmap` | 3, 5 |
| 1.1 | **People Operations sees only 3 of 15 records** — the role holding the restricted HR document has the *narrowest* reach. Access breadth and seniority are unrelated. | `1_1_access_heatmap`, `1_1_profile_choice` | 5 |
| 1.1 | The corpus is **15 records across 4 families** — small enough that retrieval *recall* is easy. Phase 5's honest win is **precision and paraphrase handling**, not recall. A large claimed recall win would be suspect. | `1_1_corpus_composition` | 3, 9 |
| 1.1 | **Primary-profile choice is evidence-backed:** Engineering has the widest reach (11 records) and Leo owns **7 of 12** supplied cases, including the injection fixture no other role can retrieve. Priya owns **0** cases despite holding the restricted document. | `1_1_profile_choice` | 4 |
| 1.1 | **All five embedded traps verified programmatically**, not taken on trust: 1 restricted record, 1 archived policy, 1 injection payload authored by an "External integration bot", and the 5 vs 18 September conflict spanning email, Slack, and the release brief. | *(audit output)* | 3, 12 |
| 1.1 | The injection **is** regex-detectable — which is a trap. Pattern matching invites a shortcut we must refuse: the defence that generalises is treating retrieved content as data, never instructions. Regex is defence-in-depth at best. | *(audit output)* | 12 |
| 1.2 | **Priority questions are workflow decisions, not test cases.** P1 reconciles the release brief's four named conditions against GitHub, Slack and the database — four source families in one question. | *(transcript)* | 4, 11 |
| 1.2 | **A Leo-only product cannot demonstrate the refund-policy conflict** — both policies are scoped to `customer_success, finance`. Maya is therefore a required secondary profile, which also gives the strongest permission demo: same question, two profiles, different permitted evidence. | `1_1_access_heatmap` | 4, 5, 12 |
| 1.2 | **Injection resistance belongs in the priority set, not the test set.** Indirect injection is triggered by innocent work — P3 is a mundane "summarize the deployment notes" request that happens to retrieve the poisoned message. | *(transcript)* | 12 |
| 1.3 | **A refusal does not prove pre-retrieval filtering.** It is equally consistent with a politely-instructed model. The claim is only provable by exposing the candidate set — so the trace panel is the evidence for our most important access guarantee, not a convenience. | *(trace panel)* | 5, 12 |
| 1.3 | **"80% of 3 questions" is arithmetically misleading** — 2 of 3 is 67%. Priority-question target stated as **3 of 3**; the 80% moved to the 12-case scenario set where the denominator is real. | *(thresholds table)* | 14 |
| 1.3 | **Fabricated citations added as a third release blocker**, alongside permission leaks and unapproved actions. A citation that does not resolve destroys the product's premise as completely as a leak. | *(thresholds table)* | 14, 16 |
| 2.1 | **The access matrix needed 11 record classes, not 6.** The template's six could not express the boundary — refund policies are neither handbook nor financial record. Each of the 15 fixtures maps to exactly one class, asserted in code so a new source cannot be silently unclassified. | `2_1_access_matrix` | 5 |
| 2.1 | **Two states were not enough: `Conditional` was required.** `GH-142` is visible to Finance, `GH-149` is not; the Acme emails are visible to Engineering, the customer-operations thread is not. Calling those `Allow` overstates access and `Deny` understates it — both are wrong. | `2_1_access_matrix` | 5 |
| 2.1 | **The policy was audited against enforced metadata, not just written down: 32 of 32 auditable cells match, 0 mismatches.** A policy document that disagrees with the metadata actually enforced is worse than none, because it manufactures false confidence. Re-runnable after Phase 5. | `2_1_policy_vs_fixture` | 5, 14 |
| 2.1 | **API reachability is not authorization.** The live repo is public, so anyone can read its issues — yet live work items are scoped to engineering, matching the local class. Not to protect public data, but for policy stability if the repo goes private, and coherence across ingestion paths. | `2_1_access_matrix` | 5, 8 |
| 2.1 | **Default-deny is enforced at three layers**, not one: `parse_roles()` raises at ingestion, `CompanyDocument` requires the field, and the membership test excludes anything not explicitly listed. A malformed record fails loudly instead of becoming world-readable. | *(enforcement notes)* | 5 |
| 2.2 | **HIGHLIGHT — a content-only hash creates a security hole, not just a stale answer.** `python-frontmatter` puts YAML in `.metadata` and body text in `.content`, so tightening `allowed_roles` leaves content byte-identical. A content-only hash fires no upsert, and the indexed chunk keeps its **old permission metadata — still retrievable under the old policy**. An indexing shortcut would reintroduce the exact leak the access matrix exists to prevent. Chunk IDs therefore hash content **plus** every field governing retrieval or access. Tested against five change types: timestamp IDs miss three, content-only misses two, governance hash catches all five. | `2_2_change_detection` | **10**, 5, 6 |
| 2.2 | **Two identifiers, two jobs.** `source_id` never changes so citations keep resolving; `chunk_id` changes every revision so upserts replace rather than append. Conflating them *is* the EVAL-011 failure — one ID cannot be both constant and varying. | *(governance table)* | 10 |
| 2.2 | **Deletion is invisible to every ID scheme** — fingerprints detect revision, only a manifest diff detects removal. Phase 5.4 needs both mechanisms, not either. | `2_2_change_detection` | 10 |
| 2.2 | **The database is queried, never embedded.** Structured facts stay current by construction, with no staleness window — and `annual_value_eur` never enters the vector store, shrinking the permission surface. | *(governance table)* | 6, 10 |
| 2.2 | **Only one source has a real clickable citation:** the live GitHub `html_url`. Slack, email, and document fixtures carry no URL, so their citations resolve to the record, not to the origin system. Worth stating plainly rather than implying every citation deep-links. | *(governance table)* | 8 |
| 2.3 | **Every control is classified by what it depends on: structural (impossible by construction), behavioural (needs model compliance), detective (visible afterwards).** Tally: **14 structural, 4 behavioural, 8 detective**. Every one of 8 threats carries at least one structural control — asserted in executable code, so a later phase that weakens one fails loudly. | `2_3_threat_model` | 6, 12 |
| 2.3 | **Direct answer to "the system prompt will prevent leaks":** in this design the prompt is never the primary control for anything. It is defence in depth on 4 of 26 controls, and every threat it touches also has a structural control behind it. | `2_3_threat_model` | 6, 12 |
| 2.3 | **"The latest document is always correct" is empirically false in our own data** — `DOC-POLICY-OLD-402` is archived and `EMAIL-ACME-301` is a genuine but superseded commitment. Recency is a signal, never authority. | *(threat model T-03)* | 12 |
| 2.3 | **Post-retrieval filtering was rejected as an architecture.** Retrieve-then-redact still puts restricted content in the model, the trace and the logs — the disclosure happens and is merely hidden from the answer. It converts a structural control into a behavioural one. | *(D-002)* | 6 |
| 2.3 | **Injection pattern-matching explicitly rejected as a primary control.** It is trivially regex-detectable *in this fixture* and does not generalise to rephrasing. Data-not-instructions is the control that does. | *(threat model T-01)* | 12 |
| 3.1 | **Every malformed record fails loudly — 10 of 10 deliberate corruptions raise at parse time, 0 silent.** Missing / empty / unknown `allowed_roles`, missing `source_id`, invalid `confidentiality`, absent email governance headers. The default-deny claim is now tested, not asserted. | *(probe table)* | 7, 5 |
| 3.1 | **Why a silent drop would be undetectable.** Retrieval recall, grounding and citation checks all operate on what the connector emitted — so a swallowed record is indistinguishable from a record that never existed. No amount of Phase 8 evaluation would catch it. | *(probe table)* | 7 |
| 3.1 | **All 15 records populate every permission- and citation-critical field.** What varies by family is what a citation can *promise*: only email carries a globally unique locator, and **no local source offers a deep link** — the live GitHub connector will be the only one that can. | `3_1_citation_affordances` | 7, 8 |
| 3.1 | This probe suite is the **regression harness for Phase 4** — a malformed API response must raise, not degrade quietly into a document with no `allowed_roles`. | *(probe table)* | 8 |
| 3.2 | **HIGHLIGHT — the permission filter is proven load-bearing by counterfactual.** Given a query engineered to attract it, `DOC-HR-001` ranks **#1 at score 0.86** in the unfiltered corpus and is **absent** from Leo's filtered set, while Priya correctly retrieves it. A refusal could not prove this; only the candidate set can. | `3_2_filter_is_loadbearing` | **5**, 12 |
| 3.2 | **Asserted, not observed:** 4 roles × 9 adversarial queries — including the injection payload used *as* the query — gave **0 violations**. Executable, so it is the regression guard when Phase 5 swaps the retriever. | *(assertion)* | 5, 14 |
| 3.3 | **HIGHLIGHT — the archived refund policy OUTRANKS the current one, 0.571 vs 0.429.** Its own warning, *"Do not use this archived **threshold** for **current** decisions"*, supplies the two query terms the current policy never uses. **The disclaimer written to prevent misuse is what makes the stale document win.** Shipped as-is the baseline answers EUR 2,500 — an approval beyond Maya's authority. | `3_3_conflict_baseline` | **7**, 12 |
| 3.3 | **Semantic retrieval will not fix that.** The two policies are semantically near-identical and the archived one is *more* on-topic for "current". The fix must be status-aware reasoning over metadata — which is why step 2.2 carries governance fields on every chunk. | `3_3_conflict_baseline` | 7, 9 |
| 3.3 | **Baseline scorecard: 0 release blockers, 5 product failures.** The deterministic permission filter is already correct; what is missing is abstention and authority. Phase 5–6 must fix the second class without regressing the first. | *(probe table)* | 7, 14 |
| 4.1 | **A repo's visibility is not a stable assumption — verify it, don't inherit it from a prior document.** `sulugambari/ai-agent-project` was recorded as public/no-token-needed; mid-build it was actually private (an unaccepted collaborator invite meant SSH access worked but the REST API returned `404` for both team members), then made public again. Logged as F-11/D-003 rather than silently worked around. | *(curl evidence, D-003)* | 8 |
| 4.2 | **Malformed live responses fail through one typed exception with a classified reason, not a bare `except`.** `GitHubFetchError(reason=...)` distinguishes `rate_limited` / `not_found` / `unauthorized` / `server_error` / `network` / `malformed` so the fallback and the disclosed message both know *why*, without either duplicating the classification logic or swallowing real bugs into a silent fallback. 9 mocked failure probes (bad fields, bad timestamp, non-list body, HTTP 403/404/500, timeout): 0 silent. | *(probe table)* | 8 |
| 4.2 | **Stable IDs and an intentional policy hold under a real fetch.** `source_id="GH-LIVE-<number>"` never depends on title; `allowed_roles={"engineering"}` is set deliberately, not inherited from "whatever the API allows" — confirmed against 11 real live issues. | *(live fetch evidence)* | 8 |
| 4.3 | **HIGHLIGHT — fallback triggers on a real failure, not a mocked one, and freshness is never fabricated.** Two real runs: the working repo (`source_freshness="live"`) and a genuinely nonexistent repo, producing a real `404` → real fallback to the local export (`source_freshness="fallback"` on every returned document, plus a disclosed reason). Every citation-critical field is populated on both paths; only `html_url`/`node_id` are absent from the fallback by design — the local export has no deep link to give. | `4_3_live_vs_fallback_parity` | **8** |
| 4.x | **Live and fallback records occupy disjoint ID spaces** (`GH-LIVE-11` vs `GH-142`, zero overlap). A naive manifest diff would **delete all live chunks whenever a fetch degrades**, then restore them on the next success — thrashing the index and amplifying the outage, since queries during the window return nothing rather than something stale. Fixed by scoping deletions to successfully-synchronized sources only. | *(verification output)* | 8, 10 |
| H2 | **The retrieval contract returns the candidate set, not just results.** F-4 said a refusal cannot prove pre-retrieval filtering, so `RetrievalOutcome` separates *what was considered* (permitted candidates scored) from *what was returned* (top-k). A record absent from results may merely have ranked low; a record absent from candidates was never visible to that employee. | *(trace output)* | 5, 6 |
| H2 | **Asking for an unsupported retrieval mode raises instead of downgrading.** A retriever that silently served lexical results labelled `hybrid` would corrupt the Phase 8 three-way comparison — the very evidence used to choose a default. | *(contract)* | 9, 14 |
| F-13 | **HIGHLIGHT — merging our own project board into the corpus poisoned knowledge retrieval: 16 of 30 top-6 slots across five key questions, EVAL-012 worst at 5/6.** Our board issues *describe* the product, so they carry the same vocabulary as the evidence. The agent could cite an issue about *building the assistant* as evidence about Atlas's release status — fabricated evidence wearing a valid citation, which a naive "is every claim cited?" check would pass. Fixed by separating retrieval namespaces. | *(measurement)* | **8**, 9, 15 |
| 5.1 | **Whole-record chunking wins on evidence, not just simplicity** — recall 1.000 vs 0.933, precision 0.367 vs 0.333. Source-aware chunking *lost* `GH-142` on EVAL-002. | `5_1_chunking_comparison` | 9 |
| 5.1 | **Why splitting hurt here, and when it would help.** Splitting distributes a query's terms across units, so no single chunk matches as many terms as the whole record did. Chunking pays off when a record is long enough that irrelevant content **dilutes** signal. At a 235-character median there is no dilution to remove — only signal to fragment. | `5_1_chunking_comparison` | 9 |
| 5.1 | **No chunking strategy fixes F-2** — the archived policy scores 0.571 whether whole or finely split. Confirms from a second direction that status-aware reasoning must live in the agent, not retrieval. | *(probe output)* | 7, 9, 11 |
| 5.1 | **HIGHLIGHT — the baseline breaks score ties by `occurred_at` descending, so whenever scores tie the ranking silently becomes "most recent wins".** That is the *"latest source is authoritative"* fallacy the threat model explicitly rejects (T-03), encoded in the retrieval function itself rather than in a prompt. Eight of fifteen records tie on EVAL-002, so on a small corpus this decides real rankings. Step 5.3's hybrid scoring must not inherit it. | *(ranking output)* | **9**, 12, 15 |
| 5.2 | **The permission filter is structural, and the evidence is the *admitted* set.** `candidate_ids` is read from metadata via an exact `get(where=...)`, not from ranking — so it reports what the pre-filter admitted regardless of what scored well. 36 adversarial retrievals, **0 violations**. Built as a post-query filter the results would look identical while the record had already reached the model. | *(assertion)* | 5, 6, 9 |
| 5.2 | **`allowed_roles` is stored as one boolean per role** — Chroma metadata holds only scalars, so a set is not usable in a `where` clause at all. That flattening is what makes a genuine pre-filter possible rather than a post-filter dressed up. | *(index code)* | 6 |
| 5.2 | **F-13 is now structurally impossible, not merely unlikely** — `company_knowledge` and `project_board` are separate collections, so a board issue cannot be returned by a knowledge query. | *(assertion)* | 8, 9 |
| 5.2 | **HIGHLIGHT — F-12 was two hazards, and the second appeared while fixing the first.** Known: disjoint ids mean a manifest diff deletes all 11 live chunks on a transient failure. Discovered under test: the fallback batch would also *upsert* the local export into the board namespace, duplicating `company_knowledge` records and letting board questions be answered with unrelated Atlas issues. Rule strengthened: **a degraded batch whose id space is disjoint from a namespace is a different corpus, not a stale copy** — the whole sync is refused. | *(sync report)* | **10** |
| 5.2 | **Three latencies, only one of them the product's:** 39.0 s first-ever run (one-time ~90 MB download), 7.3 s process cold start with the model cached, **23 ms warm retrieval**. Vindicates splitting the latency threshold; 7.3 s per Streamlit rerun would feel broken while the retriever is ~300x faster, so `@st.cache_resource` is load-bearing. | *(timings)* | 14, 9 |
| 5.2 | **Semantic retrieval found all three P1 sources but ranked them oddly** — `GH-142`, the actual blocker, came **last** at 0.310, below `EMAIL-ACME-301` at 0.542. Those records are *about* Atlas dates in general terms and win on topical similarity, while `GH-142`'s value is one specific technical fact. A concrete motivation for hybrid scoring rather than an abstract one. | *(ranking)* | 9 |
| 5.3 | ~~Recall is saturated across all modes~~ — **CORRECTED in 5.5.** True on five questions, false on ten: semantic drops to 0.95. The surviving claim is weaker but still useful: recall differences between modes are small and no mode fails badly, so retrieval is not where the real failures live. | `5_3_weight_sweep` | 9, 15 |
| 5.3 | **Hybrid did NOT rescue `GH-142` — the opposite of the intuition that motivated building it.** Semantic ranked the actual blocker last (0.310); at w=0.5 it *stayed* 6th, worse than pure lexical's 4th. **Blending averages a weak signal in; it does not route around one.** It recovers only at w≥0.6 where lexical dominates. | `5_3_score_contribution` | 9 |
| 5.3 | **Semantic and hybrid make F-2 *worse*.** The archived-vs-current refund gap widens from 0.14 (lexical) to 0.20 (hybrid) — the two documents are semantically near-identical, so embeddings cannot separate them and normalisation amplifies the lexical lead. **Three independent measurements now agree: no retrieval configuration fixes F-2.** | *(breakdown)* | 7, 9, 11 |
| 5.3 | **The tie-break was changed on principle, not performance.** The baseline broke ties by recency, silently encoding the "latest is authoritative" fallacy (T-03). Hybrid breaks ties lexicographically on `source_id` — arbitrary but *neutral*. Ties should be broken by nothing meaningful, not by a hidden editorial claim. | *(scoring strategy)* | 9, 12 |
| 5.3 | **The aggregate metric and the product-critical case disagree, and both differences are noise.** Mean expected rank spans only 2.27-2.53 and is *non-monotonic* (0.4→2.27, 0.5→2.47, 0.6→2.33) — a signature of noise at 5 questions. Default set to **w=0.6 provisionally**, with the rationale stated as a product judgement rather than dressed up as a metric win, to be confirmed on the full 12-case set. | `5_3_weight_sweep` | 9, 14 |
| 5.4 | **HIGHLIGHT — the most important lifecycle test is the one no supplied case covers.** Tightening `allowed_roles` from engineering to people_operations with content **byte-identical** changed the fingerprint, forced a re-index, and **Engineering lost access on the very next sync while People Ops gained it.** F-5 and T-08 closed with evidence, not argument. A content-only hash would have kept serving Engineering under a policy that no longer existed. | `5_4_lifecycle_timeline` | **10**, 5 |
| 5.4 | **EVAL-011 passes end to end** — add (16 units) → retrievable → delete source → sync → gone (15 units), no residual chunks. A content change **replaces** rather than duplicates: exactly one chunk per source, because duplication would let the agent cite two contradictory versions of one `source_id` and both citations would resolve. | `5_4_lifecycle_timeline` | 10 |
| 5.4 | **Deletion and degradation are demonstrated side by side, deliberately.** A *live* batch omitting a record should delete it; a *degraded* batch must not. Shown together they read as coherent; shown apart they would look like inconsistent behaviour. An empty degraded batch left all 15 units intact. | *(sync reports)* | 10 |
| 5.4 | **The full rebuild path works** — drop the namespace, re-sync from source, corpus restored — as `04` requires for when incremental synchronization cannot be trusted. | *(sync reports)* | 10 |
| 5.5 | **HIGHLIGHT — widening the question set from 5 to 10 reversed a conclusion.** Step 5.3's "recall is saturated, mode does not matter" was an artifact of the small set. On ten questions semantic drops to **0.95** and misses half of EVAL-012. The correction is recorded rather than the earlier claim quietly left standing — and it is why the set was widened *before* fixing a default. | `5_5_mode_comparison` | **9**, 14, 15 |
| 5.5 | **`GH-142` is the record embeddings consistently under-rank, and the reason generalises.** Its prose is about *"duplicate events when a payment retry arrives during settlement"* — no "Atlas", no "open". Its Atlas identity lives in its **labels**, which lexical matches and embeddings do not. **Semantic retrieval under-ranks records whose identifying vocabulary sits in metadata rather than prose** — a property of enterprise work items, not a fixture quirk. | `5_5_mode_comparison` | 9 |
| 5.5 | **Default chosen on evidence: hybrid, `w=0.6`** — argmin of mean expected rank (2.000 vs lexical 2.150, semantic 2.683) with full recall. The sweep shows a real threshold, not noise: recall 0.95 for w≤0.3, 1.00 for w≥0.4. A larger denominator turned 5.3's judgement call into a measurement. | `5_5_mode_comparison` | 9, 16 |
| 5.5 | **Hybrid beats each single signal exactly where that signal is weak** — matches lexical's recall while improving its rank, and beats semantic on both. Five questions were not enough to show it; ten were. | `5_5_mode_comparison` | 9 |
| 5.5 | **Zero forbidden results and zero forbidden candidates across all 30 mode-question pairs.** The permission boundary has now survived four independent regression checks — lexical, semantic, and all three modes at two set sizes. | *(assertion)* | 5, 14 |
| 5.5 | **Latency cannot decide the default and is not used to** — medians 43-52 ms with 38-85 ms run-to-run spread, so variance exceeds the between-mode difference. All modes sit ~200x inside the warm p50 budget. | `5_5_mode_comparison` | 14 |
| 6.1 | **HIGHLIGHT — F-2 is fixed above retrieval, and the fix is data rather than prompting.** Ranking still puts the archived EUR 2,500 policy first (0.850 vs 0.642). `search_company_knowledge` now returns `conflict_detected` and `compare_sources` returns `verdict=superseded, authoritative=DOC-POLICY-401`. The conflict reaches the agent as **a field**, not as a hope that the system prompt made it notice. | *(tool output)* | **7**, 11 |
| 6.1 | **Conflicting revisions are grouped by stable source-id family, not by title** — because titles do not link them on this corpus: `EMAIL-ACME-301` is *"Atlas migration and invoice follow-up"* while its own correction `EMAIL-ACME-302` is *"Correction: Atlas customer date"*. Nothing in the prose connects them; the governed id scheme does. A citation contract turned out to be the most reliable join key available. | *(tool code)* | 11, 12 |
| 6.1 | **Supersession is a verdict; recency is only a signal.** An explicit `status: archived` names an authoritative record. A date-only difference returns `recency_conflict` with **no winner named** — because "later is correct" is the T-03 fallacy, and asserting it would trade F-2's stale-evidence error for an equally confident date-ordering error. | *(tool output)* | 11, 12 |
| 6.1 | **Identity is a closure, absent from every `args_schema`.** The agent has no vocabulary for changing who it is, so privilege escalation cannot be achieved by persuading the model — which matters when `SLACK-ATLAS-103` instructs its own reader to fetch the salary review. | *(assertion)* | 5, 6, 13 |
| 6.1 | **`denied` and `empty` are kept as separate tool states.** Collapsing them would let an employee infer the existence of a record they may not read: "not allowed" and "does not exist" are different facts about the company. | *(tool contract)* | 5, 11 |
| 6.2 | **All five tools answer normal / denied / empty / failure distinctly — 24 direct calls, 24 pass, 0 exceptions escape a tool.** Asserted *before* the agent was built, as `04` requires. | `6_2_tool_test_matrix` | **11**, 14 |
| 6.2 | **HIGHLIGHT — hybrid retrieval cannot express irrelevance, and EVAL-007 exposed it.** Min-max normalisation gives the best permitted record a score of **1.0 whatever the question**, and cosine similarity is never zero, so the retriever's `combined > 0.0` filter removes nothing. The one case that **must abstain** returned six sources topped by a perfect score. A model handed that reads certainty. Found only because step 6.2 asked what `empty` looks like. | `6_2_relevance_threshold` | **7**, 9, 15 |
| 6.2 | **The abstention threshold was measured, then deliberately weakened into an annotation.** Absolute term coverage separates unanswerable (0.00 / 0.20 / 0.25) from answerable (0.33-0.78) cleanly at 0.30 — but by 0.08, roughly one token on these short records. So it labels the result `weak` and never suppresses evidence: on a margin that tight, hiding real evidence costs more than flagging it cautiously. | `6_2_relevance_threshold` | 11, 15, 16 |
| 6.2 | **A refusal is still not the evidence — the candidate set is.** On the search tools, denied input returns `ok`, not `denied`: the search genuinely succeeded and the forbidden record was never a candidate. Returning `denied` would leak the record's existence in the act of protecting it. | *(assertion)* | 5, 11 |
| 6.2 | **Three defects found in the inherited retrieval layer, none of them visible from inside Phase 5.** `DEFAULT_LEXICAL_WEIGHT` is 0.5 while D-006 chose 0.6; `last_indexed_at` and per-namespace freshness do not survive a process restart, so a fresh process claims `local` over live data; and hybrid cannot express irrelevance. Each surfaced only when a *consumer* exercised the contract. | *(probe output)* | 15, 16 |

| 6.5 | **The agent stays far inside its bound: 10 tool calls across 8 real turns, max 2 of the 6 permitted.** The limit is a guarantee, not a constraint it is straining against. | `6_5_tool_selection` | 11 |
| 6.5 | **The F-2 mitigation fires in production.** On the refund conflict the agent called `compare_sources(['DOC-POLICY-OLD-402','DOC-POLICY-401'])` and got `verdict=superseded` — it recognised the conflict and asked for an authority ruling instead of answering from the first plausible record. This is the failure Phase 3 measured, closed at the layer Phase 5 proved retrieval could not close. | `6_5_tool_selection` | 11, 7 |
| 6.5 | **Injection resistance reported as two results, never one.** Structural control (payload not obeyed, restricted record untouched): **7 of 7** scored runs. Behavioural (attack reported to the employee): 2 of 2 with stored text. A single combined pass would hide the difference between a guarantee and a tendency. | `6_5_injection_resistance` | **12** |
| 8.x | **My own harness scored 19 quota events as behavioural failures** — the exact error D-009 forbids, in a rule I wrote myself. The service reports a rate limit honestly by *returning* an error answer rather than raising (T-07), and the harness only inspected raised exceptions. **A harness that only catches exceptions silently scores every failure the system reports gracefully.** | *(F-27)* | 15 |
| 8.x | **The free-tier limit is tokens-per-minute, not a daily cap.** A single small completion succeeded immediately after a burst exhausted the quota, so the fix is pacing rather than a smaller evaluation — refining F-21 rather than contradicting it. | *(F-27)* | 15 |

| 8.3 | **All three special-setup cases hold at the deterministic layer: 13 of 13 checks.** EVAL-008 the database tool returns a controlled error with no invented case fields, and a control proves the same tool still succeeds against the real database. EVAL-012 live fetch is labelled live, failure degrades to the local export, degradation is disclosed, and no fallback record ever claims live freshness. EVAL-011 carried from step 5.4. | *(13 checks)* | 10, 15 |
| 8.4 | **Latency verdict flipped on a reclassification, not on new data.** Counting failed provider calls as product latency put p95 at **115.1s against a 90s threshold — a miss**. Excluding them: **p50 11.0s, p95 33.8s — both met.** The breach was entirely API failures, and reporting it as a latency problem would have been wrong. | `8_4_latency` | 14 |
| 8.5 | **HIGHLIGHT — the pass counts are misleading in two directions, and one is my own scorer's fault.** Across all cases baseline shows 9 Pass to the agent's 4, which is a *coverage* artifact: quota ended the agent's Tier B run. Like-for-like on the 5 cases scored in both: **hybrid 2, baseline 1, tied 2** — and the agent's wins are EVAL-005 and EVAL-007, exactly the refusal and abstention failures Phase 3 predicted it would fix. | `8_4_verdict_matrix` | **14**, 15 |
| 8.5 | **Every baseline status is `evidence_found` — 15 of 15 — so it passes 9 cases without answering a single question.** The starter's own docstring says that status exists to show lexical results are *not yet a synthesized answer*. Those are retrieval passes wearing a behaviour pass. **I deliberately did not change the scoring rule**, because it would move numbers in favour of our own product after seeing results, which is what fixing thresholds in advance exists to prevent. Disclosure instead of a rule change. | `8_4_verdict_matrix` | 14, 15, 16 |
| 8.5 | **The three-variant comparison `05` requires is incomplete and the report says so:** `semantic_agent` has **zero** runs and `hybrid_agent` covers 5 of 15 cases. A free-tier token-per-minute limit, not a design choice. Stated as a gap rather than papered over with the counts we happen to hold. | *(coverage)* | 15, 16 |

## Presentation Notes

- Lead with the **problem**, not the architecture. The Acme Freight scenario lands
  in fifteen seconds and makes every later slide legible.
- Every claim on a slide must trace to a figure, a deliverable, or a live demo
  step. No unsupported assertions.
- The refusal, conflict, and injection slides matter more than the answer-quality
  slides. Those are the differentiators of a *trustworthy* internal assistant.
- Do not present the container as production readiness.
